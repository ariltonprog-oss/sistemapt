.linha-formulario-tres-colunas { 
    display: grid; 
    grid-template-columns: repeat(3, 1fr); 
    gap: 20px; 
    margin-bottom: 20px; 
    width: 100%; 
}
.form-group { display: flex; flex-direction: column; gap: 8px; }
.form-group label { font-size: 0.85rem; color: var(--cor-cinza); font-weight: 500; }
.form-group input, .form-group select { width: 100%; padding: 12px; background-color: var(--borda); border: 1px solid #475569; color: var(--cor-texto); border-radius: 4px; }

.btn-submit { width: 100%; background: var(--cor-primaria); color: var(--cor-fundo); border: none; padding: 12px; border-radius: 4px; font-weight: bold; cursor: pointer; margin-top: 15px; }
.btn-submit:hover { background: #ca8a04; }

.oculto-seguranca { position: absolute; opacity: 0; height: 0; width: 0; overflow: hidden; }
/* Correção para dropdowns não ficarem escondidos */
.form-group select {
    position: relative;
    z-index: 100;
}

/* Isso garante que nenhum container esconda o menu do select */
.card, .main-content, .linha-formulario-tres-colunas {
    overflow: visible !important;
}
/* 1. Força o dropdown a ignorar o corte do container pai */
select {
    overflow: visible !important;
}

/* 2. Remove o limite de altura que possa estar escondendo o conteúdo */
.card, .form-group, .linha-formulario-tres-colunas {
    overflow: visible !important;
}

/* 3. Ajuste fino para garantir que o menu fique sobreposto a tudo */
.form-group {
    position: relative;
    z-index: 1;
}

select:focus {
    z-index: 9999; /* Traz para a frente absoluta quando clicado */
}
/* Ajustes de alinhamento e estrutura vindos do layout original */
.linha-formulario-tres-colunas {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    margin-bottom: 25px; /* Mantém o espaçamento original */
    width: 100%;
}

/* Garante que o checkbox não quebre o layout */
.alinhamento-checkbox-label {
    display: flex;
    align-items: center;
    gap: 10px;
}

/* Força a visibilidade do select (dropdown) */
select {
    overflow: visible !important;
}

.form-group {
    overflow: visible !important;
}
/* Nova estrutura para linhas que possuem apenas 2 campos */
.linha-formulario-duas-colunas {
    display: grid;
    grid-template-columns: repeat(2, 1fr); /* Apenas 2 colunas */
    gap: 20px;
    margin-bottom: 25px;
}
/* Força o título a ocupar a largura total e ficar acima do conteúdo */
header {
    width: 100%;
    display: block; /* Muda de flex para block para empilhar */
    padding: 20px 30px;
    background-color: #1e293b;
    border-bottom: 4px solid #eab308; /* Cor amarela da sidebar */
}

/* Título com a mesma cor amarela e estilo da sidebar */
header h1 {
    color: #eab308; 
    font-size: 24px;
    margin: 0;
}

/* Garante que o card comece logo abaixo */
.container {
    width: 100%;
    padding-top: 20px;
}
/* Garante que o body seja o pai flex */
body {
    display: flex;
    margin: 0;
    min-height: 100vh;
}

/* O conteúdo principal ocupa todo o espaço restante e empilha o que está dentro */
.conteudo-principal {
    display: flex;
    flex-direction: column; /* Isso coloca o header em cima e o container embaixo */
    flex: 1;
    width: calc(100% - 250px); /* Ajuste conforme a largura da sua sidebar */
}

/* Garante que o header ocupe toda a largura do seu conteúdo */
header {
    width: 100%;
    background-color: #1e293b;
    padding: 20px 30px;
    border-bottom: 4px solid #eab308;
}

/* Garante que o container comece logo abaixo do header */
.container {
    width: 100%;
}